name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build market-data-service image
        run: docker build -t allocation-feed/market-data-service ./market-data-service
